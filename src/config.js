// import dotenv from 'dotenv';
// dotenv.config();

const config = {
    api_host: `http://localhost:9000`, 
    site_title: `Restaurant Store`, 
    // global_ongkir: process.env.REACT_APP_GLOBAL_ONGKIR,
    // owner: process.env.REACT_APP_OWNER, 
    // contact: process.env.REACT_APP_CONTACT
}
export { config };
